firewall精准放行必要端口
一.写入永久规则
放行http和https服务（即80和443端口）
sudo firewalld-cmd --permanet --add-service=http(这个命令是字符服务)
或多条端口sudo firewalld-cmd --permanet --add-service={http，https}
ssh服务默认已在public放行
sudo firewalld-cmd --permanet --add-service=ssh

放行自定义端口（tomcat,mysql)
sudo firewalld-cmd --permanet --add-port=8080/tcp(这个命令是端口)
sudo firewalld-cmd --permanet --add-port=3306/tcp

添加--permanet参数后，需要重载激活防火墙配置
sudo firewall-cmd --reload
列出默认区域public的所有活动规则查看
sudo firewall-cmd --list-all

二.firewall富语言rich-rule（放行ip地址操作）
放行ip 192,168.1.50访问本机的ssh服务            (添加规则簇，添加地址，添加服务名）
sudo firewalld-cmd --permanet --add-rich-rule='rule family="ipv4"source address='192,168.1.50' service name="ssh" accept'
sudo firewall-cmd --reload



