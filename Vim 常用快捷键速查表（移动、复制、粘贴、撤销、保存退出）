默认分为普通模式（按 Esc 进入）、编辑模式、命令行模式，日常操作以普通模式为主。
一、模式切换（基础）
普通模式 → 编辑模式（写入 / 修改内容）
i：在光标前插入
a：在光标后插入
o：在当前行下方新建空行并进入编辑
O：在当前行上方新建空行并进入编辑
任意模式 → 普通模式
Esc：退出编辑 / 命令模式，回到普通模式

二、光标移动（普通模式）
1. 基础移动
h：光标左移一格
j：光标下移一行
k：光标上移一行
l：光标右移一格
也可直接用键盘方向键，新手更友好
2. 快速行内跳转
0：跳到行首
$：跳到行尾
w：向后跳下一个单词首字符
b：向前跳上一个单词首字符
3. 整篇文件跳转
gg：跳到文件第一行
G：跳到文件最后一行
数字+G：跳到指定行，例 15G → 跳至第 15 行

三、删除操作（普通模式）
x：删除光标所在单个字符
dd：删除光标所在整行
d$：删除从光标位置到行尾
d0：删除从光标位置到行首
dw：删除光标所在整个单词

四、复制、剪切、粘贴
Vim 中删除 = 剪切，可直接粘贴
yy：复制当前整行
yw：复制光标所在单词
y$：复制光标到行尾内容
p：在光标下一行 / 光标后粘贴
P：在光标上一行 / 光标前粘贴
五、撤销 & 重做
u：撤销上一步操作（可连续按）
Ctrl + r：重做（恢复被撤销的内容）

六、查找（结合你之前的需求）
需在普通模式下使用
正向搜索（向下）：/关键词 + 回车
反向搜索（向上）：?关键词 + 回车
n：跳转到下一个匹配结果
N：跳转到上一个匹配结果
*：直接搜索光标所在单词（向下）
#：直接搜索光标所在单词（向上）
高亮设置
:set hlsearch 开启搜索结果高亮
:set nohlsearch 关闭高亮

七、保存 & 退出（命令行模式）
普通模式输入 : 进入命令行模式
:w：保存文件（不退出）
:w 文件名：另存为新文件
:q：退出（文件未修改 / 已保存才可执行）
:wq / :x：保存并退出（最常用）
:q!：强制退出，不保存修改（放弃所有改动）
:wq!：强制保存并退出

八、视图模式（批量选中操作）
普通模式按下对应键进入选中状态，配合删除 / 复制使用
v：字符选择（逐字符选中）
V：行选择（整行选中）
选中后：y 复制、d 删除，按 Esc 退出视图模式

九、实用小技巧
显示行号
临时显示：:set nu
取消行号：:set nonu
大小写忽略搜索
:set ignorecase：搜索不区分大小写
重复执行上一条命令
. （小数点）：重复上一次修改 / 删除操作
