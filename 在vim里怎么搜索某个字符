Vim 搜索必须在普通模式（Normal 模式），按一下 Esc 就回到这个模式。
一.正向 / 反向搜索
向下（往后）搜索
/你要找的字

向上（往前）搜索
?你要找的字
按回车，从当前位置往文件头找。

二.跳到下一个 / 上一个匹配
n：继续往同一方向找下一个
N：往反方向找（上一个）
例子：
/error   回车
n        下一个 error
N        回到上一个 error


三、直接搜索光标所在单词
*：搜索光标下的单词（向下）
#：搜索光标下的单词（向上）

四、让结果高亮（很实用）
打开高亮：
:set hlsearch
关掉高亮：
:set nohlsearch

六、搜索特殊字符（如 * . / \ [ ]）
要加反斜杠 ** 转义，例如找一个星号：
/\*
