root用户和admin用户
最小权限原则，root用户误操作rm -rf/会导致系统崩溃
只给完成工作必需最小权限
su -i是以 root 用户身份启动一个登录 Shell，提示符通常会从：$变成：#表示你已经进入 root 环境。
查看当前是否为 root输入whoami,输出root说明当前在 root 用户下

who / w
解释：查看当前登录的所有用户。

useradd 用户名
解释：新建普通用户

userdel 用户名
解释：删除用户。

sudo 命令
解释：普通用户临时借用 root 权限执行命令。
示例：sudo mkdir /root/test

创建user_ops+sudo临时提权，高权限操作都得监控
############
版本rocky 9
创建用户
（root 执行）sudo adduser admin
提权（把admin加入到关键用户组里wheel，这个组默认拥有执行sudo的特权）
sudo usermod -aG wheel admin
加到权限组之后，切换到用户admin
su - admin切换用户
需要密码
这时候两种方式切换到admin
一：输入密码
如果知道
二：不知道密码
先进入root用户
sudo -i
进入root用户，再执行su -admin由此可见root权限极高
这里就进入到damin用户里了，输入sudo passwd修改密码
输入要设置的密码123456
如果再admin下不能修改密码
退出某个用户，返回root用户快捷键Ctrl + D或exit
切换到root用户，输入passwd admin
设置admin的新密码
再切到admin验证
###########
文件权限（重点）
Linux 权限分：属主、属组、其他用户，权限：读 (r)、写 (w)、执行 (x)
最终权限值 = 对应权限数值相加，每组权限由 3 位数字组成依次对应：第一位属主 的权限(所有者) → 第二位属组的权限 → 第三位其他用户的权限
只有读：r-- → 4
只有写：-w- → 2
只有执行：--x → 1

chmod 权限值 文件/目录           modle
解释：修改文件权限。
常用数值：
chmod 755 文件夹         目录常用权限
755（目录 / 程序常用）
属主：rwx = 7
属组：r-x = 5
其他：r-x = 5
权限：所有者可读写执行，其他人可读、执行
chmod 644 文件          普通文件常用权限
属主：rw- = 6
属组：r-- = 4
其他：r-- = 4
权限：所有者可读写，其他人仅可读
777（全开权限，生产环境慎用）
所有用户都拥有 rwx 全部权限

chown 用户:组 文件               own
解释：修改文件所属用户和用户组。
示例：chown root:root 1.txt
########
关闭通过ssh远程root访问
/etc/ssh/sshd_config
设置PermitRootlogin no
systemctl restart sshd

查看sudoers文件里组的权限
cat /etc/sudoers

打开文件并编辑文档vim /etc/ssh/sshd-config
查找PermitRootlogin看状态
看到状态PermitRootlogin被注释掉了#，或改成no了就行

再重启sshd服务systemctl restart sshd就完成了
